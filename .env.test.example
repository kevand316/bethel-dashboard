# Test credentials — currently uses the same Supabase project as production,
# with isolated pre-created test user accounts.
# Tests must NEVER run without the URL guard in tests/fixtures/users.js. See .claude/rules/testing.md.

SUPABASE_URL=https://yqgccykbdihsjqlapghr.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here

# Test user credentials — pre-created in Supabase Auth, never used in production.
TEST_USER_A_EMAIL=playwright-a@bethel.test
TEST_USER_A_PASSWORD=set-a-strong-password
TEST_USER_B_EMAIL=playwright-b@bethel.test
TEST_USER_B_PASSWORD=set-a-strong-password
